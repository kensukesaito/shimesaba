
import * as graphqlScalars from 'graphql-scalars'

export { default as schema } from './schema.js'

export const typeDefs = graphqlScalars.typeDefs

export const resolvers = graphqlScalars.resolvers
